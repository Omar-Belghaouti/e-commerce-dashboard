import { StyleSheet } from "react-native";

export const ButtonStyles = StyleSheet.create({
  container: {
    backgroundColor: "#333",
    borderRadius: 5,
    padding: 10,
    margin: 5,
  },
  title: {
    fontSize: 16,
    color: "#fff",
  },
});
